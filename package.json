{
  "name": "cortex",
  "version": "1.0.0",
  "private": true,
  "description": "Cortex - A high-performance programming language built from scratch",
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "build": "tsc -b",
    "test": "vitest run",
    "start": "npm run start --workspace=@cortex/cli",
    "clean": "tsc -b --clean"
  },
  "devDependencies": {
    "@types/node": "^20.0.0",
    "typescript": "^5.4.0",
    "tsx": "^4.0.0",
    "vitest": "^1.4.0"
  }
}
