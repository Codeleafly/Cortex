[package]
name = "nox-runtime"
version = "1.0.1"
edition = "2021"

[dependencies]
nox-shared = { path = "../shared" }
reqwest = { version = "0.13.3", features = ["json", "blocking"] }
serde_json = "1.0.149"
tokio = { version = "1.35", features = ["full"] }
